#include<bits/stdc++.h>
using namespace std;
#define case true
#define endl '\n'
#define int long long
const int N = 2e5 + 10;
int t = 1;

signed main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	if (case) 
		cin >> t;
	while (t--) {
		
	}
	return 0;
}