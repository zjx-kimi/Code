#include <bits/stdc++.h>
using namespace std;
const int N = 3e5 + 10;
int t, n;
struct node {
    int a, d;
    int pre, nxt;
} Arr[];
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> t;
    while(t--) {
        cin >> n;
        for (int i = 1; i < n; i++ 
    }
}